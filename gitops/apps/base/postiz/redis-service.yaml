apiVersion: v1
kind: Service
metadata:
  name: postiz-redis
  namespace: postiz
spec:
  selector:
    app.kubernetes.io/name: postiz
    app.kubernetes.io/component: redis
  ports:
    - name: redis
      port: 6379
      targetPort: redis
      protocol: TCP
