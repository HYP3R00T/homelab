apiVersion: v1
kind: Service
metadata:
  name: postiz
  namespace: postiz
spec:
  selector:
    app.kubernetes.io/name: postiz
    app.kubernetes.io/component: application
  ports:
    - name: http
      port: 5000
      targetPort: http
      protocol: TCP
