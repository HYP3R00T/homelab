apiVersion: v1
kind: Service
metadata:
  name: postiz-temporal
  namespace: postiz
spec:
  selector:
    app.kubernetes.io/name: postiz
    app.kubernetes.io/component: temporal
  ports:
    - name: grpc
      port: 7233
      targetPort: grpc
      protocol: TCP
